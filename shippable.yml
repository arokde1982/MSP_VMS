# language setting
language: node_js

# version numbers, testing against two versions of node
node_js:
    - 4.2.1
    
# Mongo binds to 127.0.0.1 by default
services:
  - mongodb    

script:
    - gulp